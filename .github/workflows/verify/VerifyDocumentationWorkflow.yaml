# Workflow to verify the documentation of the project.
---
name: Verify documentation.
on:
    push:
        paths:
            - "apps/docs/**"
            - ".github/workflows/verify/VerifyDocumentationWorkflow.yaml"
    pull_request:
        paths:
            - "apps/docs/**"
            - ".github/workflows/verify/VerifyDocumentationWorkflow.yaml"
permissions:
    contents: read
defaults:
    run:
        working-directory: apps/docs
        shell: bash
jobs:
    verify-documentation:
        name: Verify documentation.
        runs-on: ubuntu-latest
        env:
            NODE_ENV: test
        steps:
            - name: Checkout repository.
              uses: actions/checkout@v7
            - name: Setup Bun.
              uses: oven-sh/setup-bun@v2
            - name: Install dependencies.
              run: bun install --frozen-lockfile
            - name: Initialize testing environment.
              run: bun run test:init
            - name: Check formatting of all files using Prettier.
              run: bun run format:check
            - name: Check linting of all files using ESLint.
              run: bun run lint
            - name: Check linting of Dockerfile using Hadolint.
              uses: hadolint/hadolint-action@v3.3.0
              with:
                  dockerfile: apps/docs/Dockerfile
                  config: apps/docs/.hadolint.yaml
            - name: Run tests.
              run: bun run test
            - name: Build production-ready website.
              run: bun run build
              env:
                  NODE_ENV: production
