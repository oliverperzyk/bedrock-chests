import type { JSX } from "react"

/**
 * @summary Guide on adding custom menus to the library.
 * @description This subsite breaks down, how you can use `BaseFormData` class from library to add your custom forms.
 * @returns Content of the guide on adding custom menus.
 */
function GuideCustomMenuPage(): JSX.Element {
    return <></>
}

export default GuideCustomMenuPage
